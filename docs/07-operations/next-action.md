# Next Action

Bedrock native-world and Education knowledge have both advanced.

Implemented:

1. LevelDB actor/digest/chunk keyspace classification;
2. documented chunk-data tag recognition;
3. chunk coordinate/dimension extraction from recognized key layouts;
4. @initiator selector support;
5. typed dialogue open/change command effects;
6. NPC dialogue multiplayer and chunk-lifecycle knowledge;
7. Education Code Builder and Agent profile knowledge.

Next priority:

1. connect LevelDB keyspace scanning into inspectArtifact using a safe DB snapshot;
2. summarize actor/block-entity/pending-tick/random-tick evidence in inspection output;
3. use chunk coordinates to correlate runtime-sensitive structures/entities with native world state;
4. add dialogue scene JSON parsing and NPC scene dependency graph;
5. expand Education block/profile knowledge only where official semantics are documented.

Do not decode unknown LevelDB values without a versioned value schema.
