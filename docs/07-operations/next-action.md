# Next Action

Current phase: artifact/archive foundation established at contract level.

Next architecture topic: **Normalized Project Model + Dependency Graph**.

That discussion must define:

1. canonical project/component identities;
2. raw file inventory vs normalized semantic nodes;
3. artifact graph vs semantic dependency graph;
4. reference edge types;
5. unresolved/ambiguous references;
6. incremental indexing and change-scoped invalidation;
7. query surfaces needed by inspection and diagnostics;
8. how arena/multiplayer topology is represented without becoming a special-case core model.

ZIP transport/extraction implementation remains intentionally deferred until the safety boundary and project model contracts are stable enough to select the minimum suitable adapter.
