# Next Action

Integrated inspection now includes command state-scope and conservative repeated-topology diagnostics.

Current high-value sequence:

1. integrated manifest/function/script/structure/world-DB inspection — implemented;
2. cross-domain script/manifest and structure diagnostics — implemented initial core;
3. topology + state-scope diagnostics integration — implemented;
4. diagnostic-to-repair planning for safely repairable classes — next;
5. richer world-DB semantic decoders only from reproduced map needs;
6. additional command/API compatibility semantics only from real diagnostics.

The next repair work should start with classes that have deterministic evidence and a reversible transform, such as a proven linear coordinate outlier. Broad state-scope findings remain diagnostic-only until isolation intent is explicitly established.
