# Next Action

Static structure destinations are now correlated with native LevelDB chunk evidence.

Implemented:

1. absolute structure-load destination → chunk coordinate mapping;
2. bounded native per-chunk signal retention;
3. recognized chunk-data kinds grouped per chunk;
4. cross-dimension X/Z correlation without inventing execution dimension;
5. explicit correlation confidence boundary.

Next priority:

1. correlate embedded command-block world coordinates with native BlockEntity chunk evidence;
2. surface PendingTicks/RandomTicks evidence near runtime-significant structures;
3. add differential native-world snapshots before/after repair or Minecraft update;
4. parse NPC dialogue scene JSON and connect scene commands to graph;
5. expand Education world mechanics where they materially affect diagnosis.

Native chunk correlation is evidence, not proof of runtime activation.
