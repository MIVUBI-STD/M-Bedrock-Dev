# Analysis

Canonical semantic-analysis policy.

Owns:

- normalized project facts;
- semantic dependency relationships;
- function/command/reference semantics;
- coordinate/state/topology derivation;
- analysis evidence and traceability.

Analyzers are read-only. Diagnostics are derived from supported facts, not guesses.
