# Packages

This directory owns reusable deterministic engine modules. Package boundaries follow semantic ownership, not file type convenience.

Planned modules are documented in `docs/06-system/architecture.md`.
