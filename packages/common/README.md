# Common

Small dependency-neutral primitives that are genuinely shared across canonical owners.

Current scope is limited to generic spatial value types.

Do not turn this package into a generic `utils` bucket. A type/function belongs here only when it is domain-neutral, stable, and already needed by multiple semantic owners.
